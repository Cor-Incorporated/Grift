# Sprint N+5 Day3 Progress / 2026-02-13

## 実装テーマ
- Intake Queue の運用詰まりを可視化・自動記録
- Ready Packet からエンジニア着手までを 1 クリック化

## 実装内容
1. 一括概算ログの永続化
   - `estimate_batch_runs` テーブルを追加
   - `POST /api/change-requests/estimate-batch-runs` を追加
   - UI一括概算実行後に run ID を表示
2. Intake Queue の並び最適化
   - 緊急度（impact）→期限（requested_deadline_at）→不足量（missing_fields）で自動ソート
   - `requested_deadline / requested_deadline_at` を change request に保存
3. Ready Packet から直接タスク化
   - `execution_tasks` テーブルを追加
   - `POST /api/change-requests/:id/taskize` を追加
   - ダイアログから「このパケットをタスク化」を実行可能化

## 期待効果
- PM/営業が「一括概算を回したが失敗理由が残らない」状態を解消
- 優先処理すべきチケットが上位に集まり、手動並び替えを不要化
- 概算 ready の案件をそのままエンジニア実行キューへ渡せる

## 次の実装候補（Day4）
1. 実行タスク一覧ページ（`/admin/execution-tasks`）を追加
2. `execution_tasks` のステータス遷移 API と監査ログ
3. 一括概算 run 履歴の可視化（成功率/失敗理由の集計）
