# Sprint N+6 Day4 Progress / 2026-02-13

## 実装テーマ
- 登壇デモの実行証跡を残す

## 実装内容
1. Demo Run Log 永続化
   - `intake_demo_runs` テーブル追加
   - demo-run API 実行時に run ログを保存
2. Intake UI で demo run 履歴表示
   - 実行ID / case / parser / created件数 / 実行時刻を表示
   - run ID を成功メッセージに表示

## 期待効果
- 登壇準備中に「いつどのケースを回したか」を追跡できる
- デモ失敗時の再現手がかりを管理画面に残せる

## 次候補
1. demo run 履歴から対象チケットへジャンプ
2. demo run の失敗ログ保持（error payload）
3. demo run をシナリオ順に一括実行するバッチボタン
