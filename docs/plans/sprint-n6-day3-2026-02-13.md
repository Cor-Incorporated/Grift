# Sprint N+6 Day3 Progress / 2026-02-13

## 実装テーマ
- 登壇デモをワンクリックで再現可能にする

## 実装内容
1. Demo Runner API
   - `POST /api/intake/demo-run` を追加
   - 指定した demo case を heuristic 固定で parse→ingest し `change_requests` を自動起票
2. Intake UI 連携
   - Intake Workspace に「デモケースを即起票」ボタンを追加
   - デモ入力投入後、ワンクリックでキュー投入まで完了
3. 監査ログ拡張
   - `intake.demo_run` を required actions に追加

## 期待効果
- 登壇中に手入力せずに3ケースを即投入できる
- 依頼分解〜キュー化までのデモ導線が短縮される

## 次候補
1. デモランナーの結果サマリ画面（caseごとの成功/失敗）
2. ワンクリックで「概算生成まで」進めるデモモード
3. デモセッションを録画しやすい固定順序実行
