# Sprint N+5 Day2 Progress / 2026-02-13

## 実装テーマ
- Dashboard Only MVP の運用実装を前進
- `ready_to_start` から概算見積りまでを管理画面で完結

## 実装内容
1. Intake Queue に見積状態を追加
   - `latest_estimate_id`, `latest_estimate_status` を表示
2. Intake Queue から個別概算生成
   - `ready_to_start` かつ見積未生成の項目だけ `POST /api/change-requests/:id/estimate` を実行
3. Intake Queue から一括概算生成
   - 見積未生成の `ready_to_start` を最大10件まで順次生成
4. KPIカードを拡張
   - `概算未生成` を追加し、詰まりを可視化

## 期待効果
- PM/営業がエンジニアへ渡す前に、概算の有無を即時に埋められる
- `情報不足` と `概算未生成` の2種類の詰まりを同一画面で解消できる

## 次の実装候補（Day3）
1. 一括概算の結果ログ（成功/失敗ID）をテーブル保存
2. Intake Queue の並び順を「緊急度・期限・不足量」で最適化
3. `ready-packet` から直接タスク化する導線（内部運用向け）
