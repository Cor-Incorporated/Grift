# Sprint N+6 Day2 Progress / 2026-02-13

## 実装テーマ
- 登壇デモの再現性を高める

## 実装内容
1. Parser Mode 制御
   - intake parse/ingest API に `parser_mode` (`auto` / `heuristic`) を追加
   - `PO_INTAKE_PARSER_MODE` でも既定モード制御可能
2. デモケース投入
   - 3つのSlack風ケースを `src/lib/intake/demo-cases.ts` で管理
   - Intake Workspace から「デモケースを投入」できるUIを追加
3. デモケース自動検証
   - heuristic固定で3ケースを解析するテストを追加
   - 期待intentが抽出されることをCIで担保

## 期待効果
- 登壇時に外部LLMの揺らぎを受けず、一定の挙動を再現できる
- デモ入力の準備時間を削減できる

## 次候補
1. デモシナリオのワンクリック実行（parse→ingest→queue確認）
2. KPI表示に「デモケース成功率」を追加
3. 3ケースのPlaywright導線テスト（ログイン済みfixture）
