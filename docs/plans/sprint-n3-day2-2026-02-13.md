# Sprint N+3 Day2 実装メモ（2026-02-13）

## 目的
- `admins` 設定更新フローを Supabase Auth 依存から Clerk 認証 + service-role API へ移行する
- 管理設定変更を監査ログに残し、GOV-102 の監査網羅性を引き上げる

## 実装内容

### 1. Admin Profile API を追加
- 追加: `src/app/api/admin/profile/route.ts`
- GET:
  - Clerk認証済み + admin権限ユーザーのみ取得可能
  - `admins` を `clerk_user_id` で取得し、未登録時はデフォルト値を返却
- PUT:
  - 入力を `adminProfileSchema` で検証
  - `admins` を `clerk_user_id` で upsert
  - `admin_profile.upsert` を監査ログへ記録

### 2. Admin Settings UI を API 経由に変更
- 更新: `src/app/admin/settings/page.tsx`
- 変更点:
  - `createClient()` + `supabase.auth.getUser()` を廃止
  - `/api/admin/profile` GET/PUT 呼び出しへ置換
  - 初期読込状態の表示と保存時バリデーションを追加

### 3. バリデーション・監査網羅性の更新
- 更新: `src/lib/utils/validation.ts`
  - `adminProfileSchema` を追加
- 更新: `src/lib/utils/__tests__/validation.test.ts`
  - `adminProfileSchema` の正常/異常系テストを追加
- 更新: `src/lib/audit/required-actions.ts`
  - `admin_profile.upsert` を required action に追加

## Day3候補
- `admins` / `team_members` の整合チェック（未リンクユーザー検出）
- RLS advisor 警告のうち policy 条件の見直し（`auth_rls_initplan`）
- Branch protection (`develop`/`main`) への `quality-gate` 必須化
